#JF Integers and Floats

"""price = float(input("How much does the item cost? $"))
tax_rate = float(input("What is the tax percent? %"))/100

total = price * (tax_rate + 1)
print(round(total, 2))"""

apples = 300
friends = 65

print(f"Each friend gets {apples//friends} apples and there will be {apples%friends} left over.")